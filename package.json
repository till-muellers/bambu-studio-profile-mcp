{
  "name": "printing-profile-mcp",
  "version": "0.1.0",
  "description": "MCP server for resolving and writing Bambu Studio process/filament profiles",
  "type": "module",
  "main": "dist/index.js",
  "bin": { "printing-profile-mcp": "dist/index.js" },
  "files": ["dist", "schema"],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
